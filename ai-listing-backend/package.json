{
  "name": "ai-listing-backend",
  "version": "1.0.0",
  "description": "Standalone microservice for the AI Listing Writer Chrome extension — generates titles/descriptions via Groq. Auth and subscription/usage limits are proxied to the shared chrome-extensions-backend so all extensions share one user base; this service never touches MongoDB directly.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
